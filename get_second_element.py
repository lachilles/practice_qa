# l = [(50, 0), (4, 5), (10, 3)]
# how to unpack tuples, looping through lists, sorted()

"""takes a tuple and returns second element"""

def get_second_element(tup):
    
    return tup[1] 


l = [50, 0, 4, 5, -1]


#implement bubble sort
# loop through list and compare if first element is less than the rest of 
# the array and keep there. do this with each number.

answer
# sorted(l, key=get_second_element)
# min or max of the list, global_min, where you are on the list, then the list

seen = l[0]
result = []

for num in l:    
    if num <= seen:
        result.append(num) 
        seen = num

tup = [(50, 0), (4, 5), (10, 3)]
sorted(iterable ,key)


sorted()


for i in range(len(l)):   # 0, 1, 2, 3, 4 (comparing one number with the one before)
    cur_num = l[i]   # 0
    for j in range(len(l)):  #  0, 1, 2, 3, 4 (all of the other numbers in the array)
        if cur_num < l[j]:
            result = cur_num



for a, b in enumerate(l):
    # 
    if b 
    return 

input[1]

if __name__ == '__main__':
    doctest.testmod()



